{
  "name": "@pww/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Firebase Cloud Functions — Cat 4 schrijf-feedback proxy (Anthropic API).",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
