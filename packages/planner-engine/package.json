{
  "name": "@pww/planner-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Planning-algoritme: meerdaagse spreiding van leerblokken vanaf vandaag, dekking vóór de herhaalweek, moeilijkheidsgewichten, herplannen bij falen. Zie SPEC.md §7.",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "echo 'TODO: eslint' && exit 0",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
