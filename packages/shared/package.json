{
  "name": "@pww/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gedeelde types en utils tussen apps en packages.",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "echo 'TODO: eslint' && exit 0",
    "test": "echo 'no tests (types only)' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
